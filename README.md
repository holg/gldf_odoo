# GLDF Odoo Integration

This is an Odoo module to integrate GLDF (Global Lighting Data Format) into Odoo.

## Installation

To install the required dependencies, run:

```sh
pip install gldf_rs_python